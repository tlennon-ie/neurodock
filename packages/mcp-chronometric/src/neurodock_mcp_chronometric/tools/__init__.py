"""Tool implementations for the chronometric MCP server.

Each module here exposes a single pure function. The :mod:`server` module wires
those functions into FastMCP. Keeping tool code separate from the framework
glue makes the tools trivially unit-testable.
"""
