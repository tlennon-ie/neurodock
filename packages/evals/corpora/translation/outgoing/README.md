# translation/outgoing/

Examples for the `rewrite_outgoing` tool. Each example carries an `input`
matching `RewriteOutgoingInput` (including a `target_register` and optional
`preserve_terms`) and an `expected` block describing the contract the rewrite
must satisfy (e.g. preserved/unpreserved terms, register shift).
