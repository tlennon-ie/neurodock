# Placeholder — versioned slice directory for tone translation corpus v0.1.0.
# Real examples live as *.example.yaml files directly under corpora/translation/tone/.
# This directory will hold general.jsonl when the slice is exported for eval runs.
