# docs/

Public documentation site. Hosted at `docs.neurodock.org`.

Astro Starlight scaffold is deferred to Phase 1, week 3 (see `plan.md` Section 15). For now, this directory hosts:

- `docs/decisions/` — Architecture Decision Records (owned by `mcp-architect`).

Once the Starlight scaffold lands, expect the standard structure: `astro.config.mjs`, `package.json`, `src/content/docs/`.
