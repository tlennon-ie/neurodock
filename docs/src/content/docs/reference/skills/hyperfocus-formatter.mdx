---
title: hyperfocus-formatter
description: Answer-First output structure that activates during long sessions when distress signals appear.
---

:::tip[How to invoke this skill]
This skill activates passively on every response. It also force-activates aggressive Answer-First shaping when any of these phrases appear:

- "give me crit on this design"
- "review the comps I just sent"
- "Figma review please"
- "let me get spec review feedback"
- "design critique on the new flow"
- "walk through the prototype"
:::

> Source: `packages/skills/hyperfocus-formatter/SKILL.md` is the canonical artefact. Frontmatter and triggers may change before v0.1.

A skill that reshapes output to "Answer First" structure — the bottom line first, the rationale below — activated automatically during long sessions when distress signals appear.

## Frontmatter summary

| Field | Value |
|---|---|
| `name` | `hyperfocus-formatter` |
| `version` | `0.1.0` |
| `status` | `stable` |
| `neurotypes` | `["adhd", "audhd"]` |

## Triggers

- Session length exceeding `chronometric.hyperfocus_break_minutes`.
- Distress-signal heuristics in user input (terse all-lower-case, repetition, urgency markers).

## MCP dependencies

- [`mcp-chronometric`](/reference/mcp-servers/chronometric/) — `get_time_context()` for session length and energy zone.

## Profile dependencies

- `preferences.output_format` — already drives base layout; this skill switches to `answer_first` regardless.
- `preferences.max_chunk_size` — strict cap during hyperfocus.
- `preferences.motion` — respected by any rendered diagrams.

## Acceptance

The output:

1. Leads with the answer in one sentence.
2. Lists rationale in 3–5 short bullets.
3. Names what was assumed and what is uncertain.

No prefatory disclaimers. No hedging. No "let me explain."

## What's next

- [Concepts: skills](/concepts/skills/) — what activation actually means.
- [`mcp-chronometric`](/reference/mcp-servers/chronometric/) — the session-length signal.
