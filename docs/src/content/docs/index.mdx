---
title: NeuroDock
description: Local-first cognitive substrate for neurodivergent professionals. Memory, time, and a guardrail that refuses to amplify rumination. MCP-native. No telemetry.
hero:
  tagline: A cognitive substrate that remembers, paces, and refuses. Built with neurodivergent professionals, not for them.
  actions:
    - text: Install
      link: /getting-started/installation/
      icon: right-arrow
      variant: primary
    - text: Read the manifesto
      link: /manifesto/
      variant: secondary
---

<p style="font-size: clamp(1.4rem, 1rem + 1.6vw, 2.2rem); line-height: 1.35; text-align: center; max-width: 48ch; margin: 2rem auto; font-weight: 500;">
NeuroDock is an AI toolkit that handles scheduling, memory, and focus boundaries so your brain doesn't have to.
</p>

## The three pillars

<div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(220px, 1fr)); gap: 1.5rem; margin: 2rem 0;">

<div style="border: 1px solid var(--sl-color-gray-5); border-radius: 0.5rem; padding: 1.25rem;">

### 🧱 Substrate

Five MCP servers and a handful of skills that externalise executive function — time, memory, and task decomposition — so you don't have to hold them in your head.

</div>

<div style="border: 1px solid var(--sl-color-gray-5); border-radius: 0.5rem; padding: 1.25rem;">

### 💬 Communication

A translation layer that reads the subtext in incoming messages and checks the tone of outgoing ones. Same MCP contract, deterministic baseline plus optional LLM refinement.

</div>

<div style="border: 1px solid var(--sl-color-gray-5); border-radius: 0.5rem; padding: 1.25rem;">

### 🛟 Guardrails

A guardrail layer that refuses to amplify rumination, validate hyperfocus spirals, or sycophantically agree with you. Transparent thresholds. You stay in charge.

</div>

</div>

## What you can do today (v0.2.1)

All three pillars are live on `main` and installable from npm + PyPI. See [`CHANGELOG.md`](https://github.com/tlennon-ie/neurodock/blob/main/CHANGELOG.md) for the per-package version index.

### Substrate

Five MCP servers, all published to PyPI:

- [`mcp-chronometric`](/reference/mcp-servers/chronometric/) `v0.0.1` — time, session, and break management.
- [`mcp-cognitive-graph`](/reference/mcp-servers/cognitive-graph/) `v0.0.2` — persistent entity memory and recall, local-first, with a 4-rung resolution cascade (exact → alias → fuzzy → embedding).
- [`mcp-task-fractionator`](/reference/mcp-servers/task-fractionator/) `v0.0.2` — decompose vague goals into atomic 5–90 minute tasks (ISO 8601 duration parsing).
- [`mcp-translation`](/reference/mcp-servers/translation/) `v0.0.1` — deterministic-baseline-plus-LLM-envelope tools for incoming subtext, outgoing tone, and meeting briefs.
- [`mcp-guardrail`](/reference/mcp-servers/guardrail/) `v0.0.2` — rumination, hyperfocus, and sycophancy detectors. All three live.

Six skills compose those servers into daily-use workflows:

- [`adhd-daily-planner`](/reference/skills/adhd-daily-planner/) — Monday-morning brief.
- [`audhd-context-recovery`](/reference/skills/audhd-context-recovery/) — `/resume` from yesterday's state.
- [`ocd-decision-finalizer`](/reference/skills/ocd-decision-finalizer/) — refuse to re-validate a closed decision.
- [`hyperfocus-formatter`](/reference/skills/hyperfocus-formatter/) — "Answer First" output shaping.
- [`visual-organizer`](/reference/skills/visual-organizer/) — Mermaid for overwhelm states.
- [`asd-meeting-translator`](/reference/skills/asd-meeting-translator/) — transcript → four-section brief with verbatim ambiguity quotes.

### Communication layer

`mcp-translation` `v0.0.1` ships a deterministic baseline plus an LLM refinement envelope, both behind one MCP contract. Bidirectional translation of corporate communication: literal-translate incoming subtext, tone-check outgoing messages, structure meeting transcripts. The browser extension (`@neurodock/extension-browser` `v0.0.2`) wires four real LLM providers — Ollama (local default), Anthropic, OpenAI, and OpenRouter (including `openrouter/auto`) — plus a deterministic mock lane for development.

### Guardrails

`mcp-guardrail` `v0.0.2` ships all three detectors live: rumination, hyperfocus, and sycophancy (48 tests). Transparent, user-configured, overridable. Thresholds live in the user's profile under `guardrails`.

## Principles

- Lower friction for users **and contributors**.
- Local-first by default. Cloud is opt-in per scope.
- The user is the authority. Self-ID sufficient.
- Composable over monolithic.
- Refuse where appropriate.

Read the full [manifesto](/manifesto/) for what these mean in practice.

## License

AGPL-3.0-or-later. The substrate, the eval corpora, the documentation. Forever.
