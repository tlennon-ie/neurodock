# profiles/

Curated profile presets ship here. Each preset is a YAML file matching the schema in `@neurodock/core`.

Profiles compose. A user's `~/.neurodock/profile.yaml` can extend a curated preset and override individual fields.

See `plan.md` Section 6 for the profile schema and Section 11 (Phase 1) for the launch presets.
