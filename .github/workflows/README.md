# GitHub Actions workflows

Actions are currently pinned to tags (e.g. `actions/checkout@v6`). Future
hardening: pin to commit SHAs once dependabot's `github-actions` ecosystem
produces SHA-based updates, so renovate-style auto-bumps still flow.
