# examples/

End-to-end demos, one per supported MCP client.

Each example is self-contained: profile file, install script, sample session, expected output. Examples must run against a fresh install in under fifteen minutes.

Phase 0 placeholder. First examples ship with the Phase 1 developer preview.
